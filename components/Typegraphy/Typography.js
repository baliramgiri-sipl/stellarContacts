export function TypographyMuted({ title }) {
    return (
        <p className="text-sm text-muted-foreground">{title}</p>
    )
}

export function TypographySmall({ title }) {
    return (
        <small className="text-sm font-medium leading-none">{title}</small>
    )
}